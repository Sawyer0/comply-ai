[pytest]
addopts = -q
markers =
    integration: integration tests that exercise multiple components (no external services)
    performance: micro-performance tests for orchestrator conflict/aggregator
